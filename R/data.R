#' @title Vignette data
#' @description Pre-cooked data used by the vignette. This data is precomputed
#'   to speed vignette compilation time and is used throughout the examples.
#'   Contains a list of two objects:
#' - `mbres`: Results from running motifbreakR on five randomly selected genes, then run through [mb_to_tibble].
#' - `mball`: Results from running motifbreakR on _all_ genes, then run through [mb_to_tibble].
"vignettedata"
